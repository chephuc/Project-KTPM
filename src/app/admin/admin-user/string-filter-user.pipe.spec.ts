import { StringFilterUserPipe } from './string-filter-user.pipe';

describe('StringFilterUserPipe', () => {
  it('create an instance', () => {
    const pipe = new StringFilterUserPipe();
    expect(pipe).toBeTruthy();
  });
});
