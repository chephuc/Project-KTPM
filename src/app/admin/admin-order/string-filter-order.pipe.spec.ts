import { StringFilterOrderPipe } from './string-filter-order.pipe';

describe('StringFilterOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new StringFilterOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
